import { Category } from "~/types/types";

export const categories: Category[] = [
  {
    id: "category-1",
    name: "First category",
    priceFormula: "Price = baseDayRental * numberOfDays * 1.5 + baseKmPrice * numberOfKm * 1.5"
  },
  {
    id: "category-2",
    name: "First category",
    priceFormula: "Price = baseDayRental * numberOfDays * 1.5 + baseKmPrice * numberOfKm * 1.5"
  }
]
